apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: csoc-app-of-apps
  namespace: argocd
  # Applied once by scripts/bootstrap/argocd/bootstrap-apps.sh.
  # After that, Argo CD manages everything under argocd/ from Git.
spec:
  project: rgds
  source:
    repoURL: https://github.com/jayadeyemi/js-poc-csoc-bootstrap
    targetRevision: master
    path: argocd
    directory:
      recurse: true
      exclude: "install/**"
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: false          # project and infrastructure retirement is always deliberate
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
